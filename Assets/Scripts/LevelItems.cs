using UnityEngine;

/**
 * Manages the item in scene.
 * TODO: at a level starts deal some items in the map, and keep the list of items.
 * TODO: all item should appears in a random clean position.
 * TODO: tile should be associated with it's item.
 * TODO: once an item disappear consumed by a root or due to the timeout then creates a new one.
 */
public class LevelItems : MonoBehaviour
{
}