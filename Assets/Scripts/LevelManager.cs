using UnityEngine;

/*
 * TODO: when the level started launch some, animations like camera animation and others before unpause the start of the game
 * TODO: generate the different items in the map.
 * TODO: assign the level values to the respective listeners.
 * TODO: once another level starts then do the same cycle.
 */
public class LevelManager : MonoBehaviour
{
    public LevelEvents levelEvents;

}