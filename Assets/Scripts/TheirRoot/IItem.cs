namespace TheirRoot
{
 /**
     * Interface to communicate with the Items, the Items are all the elements in the scene that interacts with the root,
     * this items has some positive effects for the tree like given health and others.
     * TODO: remove the item at some time
     * TODO: listen it's tile event to check if the tile is clicked in order to invoke the consume event.
     */
 public interface IItem
    {
    }
}